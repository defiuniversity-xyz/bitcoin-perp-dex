# Backend API base URL (no trailing slash).
# Leave empty or omit for same-origin (dev with Vite proxy).
# For production (e.g. Fleek): set to your backend URL e.g. https://api.yourbank.com
VITE_API_BASE=https://your-backend.example.com
