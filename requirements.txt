# Bitcoin Bank on Lightning + Nostr

# Web framework
Flask==3.0.0
Flask-CORS==4.0.0
gunicorn==21.2.0

# Nostr protocol
nostr==0.0.2
secp256k1>=0.14.0
websocket-client>=1.3.3

# Lightning (optional - for LNbits backend and invoice parsing)
requests>=2.28.0
bolt11>=0.8.0

# Utilities
python-dotenv>=1.0.0

# Yield distribution (optional - for savings yield from Lightning routing fees)
APScheduler>=3.10.0

# Greenlight node integration (optional - when YIELD_SOURCE=node)
# gl-client and pyln-grpc-proto are omitted here; install manually when using Greenlight backend
# gl-client requires Rust toolchain to build from source

# Google Cloud Storage (for SQLite persistence in Cloud Run)
google-cloud-storage>=2.10.0

# Futures DEX — price oracle HTTP calls (requests already above, no new dep needed)
