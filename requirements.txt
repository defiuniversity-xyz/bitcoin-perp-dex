# Bitcoin Bank on Lightning + Nostr

# Web framework
Flask==3.0.0
Flask-CORS==4.0.0
gunicorn==21.2.0

# Nostr protocol
nostr==0.0.2
secp256k1>=0.14.0
websocket-client>=1.3.3

# Lightning (optional - for LNbits backend and invoice parsing)
requests>=2.28.0
bolt11>=0.8.0

# Utilities
python-dotenv>=1.0.0

# Yield distribution (optional - for savings yield from Lightning routing fees)
APScheduler>=3.10.0

# Greenlight node integration (optional - when YIELD_SOURCE=node)
gl-client>=0.3.0
pyln-grpc-proto>=0.1
